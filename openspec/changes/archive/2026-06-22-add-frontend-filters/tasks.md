## 1. Room Filtering Implementation

- [x] 1.1 Add state variables in `frontend/app/page.js` for room capacity, room building, and room status filters.
- [x] 1.2 Create the UI controls (select/input elements) above the room catalog to allow users to input filter values.
- [x] 1.3 Implement the filtering logic to derive the filtered list of rooms based on the active filter states.
- [x] 1.4 Update the room catalog mapping to use the filtered list of rooms instead of the raw `salas` state.

## 2. Reservation Filtering Implementation

- [x] 2.1 Add state variables in `frontend/app/page.js` for student name and reservation date filters.
- [x] 2.2 Create the UI controls (text input, date picker) above the active reservations list to allow users to input filter values.
- [x] 2.3 Implement the filtering logic to derive the filtered list of reservations based on the active filter states.
- [x] 2.4 Update the active reservations mapping to use the filtered list of reservations instead of the raw `reservas` state.
